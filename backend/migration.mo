module {
  type OldActor = {
    // Old state fields
  };

  type NewActor = {
    // New state fields
  };

  public func run(old : OldActor) : NewActor {
    // Transform old state to new state
    {};
  };
};
